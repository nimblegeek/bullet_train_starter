en:
  application:
    name: bullet_train_starter
    owner: Unidentified Developer
    description: "You can update this description in `config/locales/en/application.en.yml`."
    keywords: bullet_train_starter seo keywords
    support_email: change.me@localhost
    key: bullet_train_starter
  webhooks/outgoing/endpoints:
    fields:
      api_version:
        options:
          1: V1
          # 🚅 super scaffolding will insert new api versions above this line.
